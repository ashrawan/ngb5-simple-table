import { StColumnSortDirective } from './st-column-sort.directive';

describe('StColumnSortDirective', () => {
  it('should create an instance', () => {
    const directive = new StColumnSortDirective();
    expect(directive).toBeTruthy();
  });
});
